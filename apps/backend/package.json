{
    "name": "backend",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "NODE_ENV=development bun --env-file=.env --hot src/app.ts",
        "db": "bun --env-file=.env prisma db push && bun --env-file=.env prisma generate"
    },
    "dependencies": {
        "@hono/zod-validator": "^0.8.0",
        "@prisma/client": "6.19",
        "@repo/utils": "workspace:*",
        "@repo/validations": "workspace:*"
    },
    "devDependencies": {
        "@repo/config": "workspace:*",
        "prisma": "6.19"
    }
}
